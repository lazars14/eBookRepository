/* Overlays for register banner */

/* Open */
function openOverlay(divName) {
    document.getElementById(divName).style.height = "100%";
}

/* Close */
function closeOverlay(divName) {
    document.getElementById(divName).style.height = "0%";
}